# The path to Ruby Enlightenment starts with the following:

$LOAD_PATH << File.dirname(__FILE__)

require "01_<%= I18n.t('tests.asserts.filename').pathmap('%n') %>"
require "02_<%= I18n.t('tests.nil.filename').pathmap('%n') %>"
require "03_<%= I18n.t('tests.objects.filename').pathmap('%n') %>"

Neo::ThePath.new.walk
